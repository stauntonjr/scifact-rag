# Issue 35 execution recovery

The owner explicitly instructed continuation through timeouts and software failures in the DGX task on September 19, and renewed the instruction to resolve the blockers and complete all 42 cases in the Mac task. This supersedes the earlier single-retry stop rule. Cases, source text, split, rubric, prompts, schemas, model assignments and qualification thresholds remain unchanged. Source-validation fixes and explicit normalization policies are recorded below.

Disposition: adapt the existing coordinator, campaign ledger, admitted Codex transport, and qualification functions. The flat recovery entry point is `tools/generation_review_recovery.py`; implementation and offline tests are `src/scifact_rag/generation_review_recovery.py` and `tests/test_generation_review_recovery.py`. Existing active role-separated-analysis and product-validation-challenges capabilities are reused. No product or public API change; release impact none.

Preserve all physical attempts from development runs 002, 005 and 008. Their seven calls contain three failures, four valid completions, and three distinct completed schedule entries. Select the earliest valid completion, never the most favorable judgment. Verify immutable source digests, start/result chains and reconstructed requests. Record source snapshots and the selected results in a fresh output root. Do not use recursive continuation descriptors or their undercounted reports as the accounting authority.

Use the same campaign ledger: 19 engineering calls, 7 development calls, 968.7196695829552 elapsed seconds, zero unknown outcomes before recovery. Retry an attributable transport or invalid-output failure in a fresh isolated session, at most five times per missing judgment, retaining every failure. Preserve the inherited 1,000 development-attempt guard and 10,800-second cumulative live ceiling; these are upper bounds, not allocated new calls. Runtime drift, unknown dispatch or identity mismatch stops admission. Missing initial/peer judgments block final adjudication. Report incomplete execution honestly if a bound is reached.

The current reviewed profile admits. The changed user configuration digest affects the ignored user-config source; enforced command configuration, executable and evidence controls remain unchanged. Historical config hashes are retained, and new attempts record the current profile hash. No global configuration modification or model substitution is part of recovery.

Independent scope/evidence review: `/root/assess_recovery` verified the seven physical calls and approved flat reconstruction. Independent code review identified current-manifest validation and fatal identity-boundary handling; both were repaired. Offline recovery tests and existing workflow tests pass (54 tests); type checking and lint pass before dispatch. A final repository gate and independent result/accounting review follow execution.

## Multi-excerpt validator repair

Live execution exposed a deterministic validator defect: rationale validation used a dictionary keyed by document ID and silently discarded earlier excerpts for duplicate IDs. The failed quote in document 18816720 occurs exactly once in an earlier supplied excerpt. The repair groups distinct excerpt texts by document ID and counts exact occurrences across them; identical duplicate excerpts deduplicate, zero matches remain invalid and multiple matches remain ambiguous. Four regression cases preserve these distinctions. No case text, generated output, model prompt, rubric or scientific threshold is edited.

Recovery001 was deliberately drained after its current call by temporarily revoking the local runtime descriptor admission, then restoring the exact saved descriptor bytes after terminal persistence and zero unknown outcomes. The maintenance evidence records original/restored hashes and accounting. This was a maintenance stop, not a new unexplained environment failure. Its executed source is retained with the run.

Recovery002 revalidates unchanged historical raw output, records each original status and current validation decision, and chooses the earliest valid result. The 13 historical calls contain five originally recorded failures; corrected validation identifies two invalid outputs and four duplicate valid completions, leaving seven distinct reusable judgments. These are different accounting views, not erased failures. Final adjudication must bind the newly selected prerequisite records. A future incompatible final cannot be reused silently.

The new runner supports a graceful SIGUSR1 drain after the active call. Independent reviewer `/root/assess_recovery` approved the lookup repair, revalidation boundary and recovery002 execution. Focused workflow/pilot/recovery suite: 83 passed. Type check: zero errors. Final full gate is repeated because the validator changed after the first passing gate.

Recovery002 completed the11 clarification r1 judgments, then stopped cleanly on an ignored user-config hash change. Independent review approved profile005 to monitor effective configuration only; the excluded file's observed hash remains audit metadata. Recovery003 resumes the same physical ledger with11 retained judgments,17 development calls,1460.8197177079492 elapsed seconds and0 unknowns.

### Quote-delimiter syntax correction

During clarification, the adjudicator repeatedly returned exact copied text surrounded by literal ASCII quotation marks inside its JSON strings. The recovery adapter now removes at most one outer double-quote pair only when the original string has zero source occurrences and the inner string has exactly one occurrence in the permitted answer or distinct same-document excerpts. No whitespace, wording, labels, spans or source text is changed. The unchanged exact-source validator then validates every field. Original raw responses and hashes remain stored; normalized fields are explicitly marked, and original failure statuses remain in revalidation audit records.

Recovery003 drained via SIGUSR1 after call39, with zero unknown outcomes. Uniform revalidation yields29 distinct judgments,5 invalid attempts and5 duplicate valid outputs across39 physical calls. The first valid chronological output is retained even when retry judgments disagree. This is a syntax-normalized development assessment, not proof that every raw model response was valid. Independent scope review approved the narrow change. Focused tests:88 passed, including exact originals, ambiguous originals/inner text, missing inner text and nested delimiters.

### Late completion of missing slots

Assessment clarification fixes did not eliminate every punctuation error. Five outputs for one assessment initial judgment ended a quoted fragment with a period where the supplied source continues with a comma. An opt-in `missing-slot-quote-syntax/v1` completion pass preserves every already selected record and its final-adjudication dependencies. It considers only missing slots, in original attempt order, allowing one ASCII/curly double-quote pair and at most one terminal period to be removed only when original text has zero matches and the candidate is exactly one unique source substring. It never repairs labels, spans, whitespace, words or paraphrases. The unchanged validator must accept the full resulting judgment.

Raw responses, original result digests, normalized field paths and the late policy identity are retained separately. New final adjudication binds any newly completed initial record. This is explicitly mixed normalization-policy provenance, not a claim of one frozen raw-output acceptance policy. Independent scope and implementation review approved this bounded operational completion; a regression test proves existing selection digests and raw source bytes are preserved. No global renormalization or outcome-driven replacement is authorized.
