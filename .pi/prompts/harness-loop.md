---
description: Execute one evidence-producing engineering loop
argument-hint: "<accepted objective>"
---
Use the `execute-engineering-loop` skill for this accepted objective: $ARGUMENTS

Resolve the governing work item and acceptance boundary, assess context readiness, and create the deterministic loop record before implementation. Give each acceptance criterion a stable ID, declare exact/prefix write scope, and record the Pi session identity as an implementer. Follow `harness/loops/engineering-loop.yaml`. Do not treat this Pi session as an independent verifier of changes it authored; use a separate trusted session or another authorized verifier for the revision-bound verdict.
